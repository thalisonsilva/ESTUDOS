package main    // Todo aquivo para ser executavel preicisa está no (package main)!!

import (       // Importação de Pacotes!! 
    "fmt"
)
func main() {
	fmt.Print("Olá Mundo!")   // Pra executar o código e só usa o comando no terminal (go run "Nome do arquivo")
}